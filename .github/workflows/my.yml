name: My First Workflow

on:
  push:

jobs:
  hello-job:

    runs-on: ubuntu-latest

    steps:

      - name: Print Message
        run: echo "Hello Sonu"

      - name: Show Date
        run: date

      - name: Show Directory
        run: pwd

      - name: List Files
        run: ls -la